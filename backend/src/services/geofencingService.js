const monitorGeofence = (truckLocation, geofence) => {
    // Compare truck location with geofence boundaries
    // Trigger alerts if a truck enters/exits the geofence
  };
  
  module.exports = {
    monitorGeofence,
  };
  